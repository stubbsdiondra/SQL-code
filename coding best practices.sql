# Cleaning coding is very important. When assigning names, always choose short, meaningful and pronouceable names
# code also needs to be readable - organize the code vertically and horizontally
# key words = BLUE
# numbers = ORANGE
# objects = BLACK
# use the brush tool to beautify your code

use sales;

CREATE TABLE IF NOT EXISTS test (
    numbers INT(10),
    words 	VARCHAR(10)
);


SELECT 
    *
FROM
    test;
    
    DROP TABLE test;

/*
Comment 1
Comment 2
*/

# Comment 4
-- Comment 4
